# Tareas del Desarrollador Junior

> Mis tareas asignadas y su estado. `agents.md` y `.agents/tasks.md` son la fuente del equipo; aquí el seguimiento personal.

## En curso
*(ninguna asignada aún)*

## Pendientes (conocidos, sin asignar)
- [ ] Implementar motores pendientes de Capa 0 con dataset precargado (ej. `excel_básico` con 200 filas en `SpreadsheetSim`) — propuesto en `memory_logs.md` como siguiente paso.
- [ ] Resolver tickets de squash de migraciones (`20260903_baseline_audit.sql`: 7/10 locales sin equivalente remoto confirmado).

## Completadas
*(ninguna aún como JR — historial previo del proyecto en `agents.md` R-08…R-15)*

## Regla
Al cerrar cada tarea: actualizar esta lista + `memoria.md`, correr `npm run test` y `npm run audit:story`, y reportar en el chat (archivos, lógica, dudas para el TPM).
